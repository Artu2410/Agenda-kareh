{
  "scripts": {
    "server:prisma:generate": "npm --prefix server run prisma:generate",
    "server:prisma:deploy": "npm --prefix server run prisma:migrate:deploy",
    "server:prisma:status": "npm --prefix server run prisma:migrate:status"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1021.0",
    "fast-xml-parser": "^5.5.9"
  }
}
