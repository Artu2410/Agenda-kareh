{
  "name": "agenda-kareh",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
  "build": "npm run build --prefix client",
  "install-client": "npm install --prefix client"
}
}
